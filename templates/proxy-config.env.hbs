# 本文件由 dsh-mcp-tunnel 生成 —— mcp-proxy 的非敏感配置（安全护栏 2）：
# 敏感项（API_KEY 访问令牌）一律走部署目录 .env（docker compose 原生读取，
# 由插件以 0600 权限生成），本文件与 docker-compose.yml 中不出现任何秘密。
# 本文件是 dsh-mcp-tunnel 声明的代理环境契约，最终以 docker-compose.yml 为准。

# 隧道面向的 MCP 上游端点（Streamable HTTP 或 SSE，容器网络内可达）。
UPSTREAM_MCP_URL={{upstreamUrl}}

# 公开侧信息（仅日志/展示用；真正对外地址是 cloudflared 的 Quick Tunnel URL）。
MCP_PROXY_EXTERNAL_HOSTNAME={{externalHostname}}

# 代理监听（容器内端口 80 已由 compose 固定，勿改）。
MCP_PROXY_HOST=127.0.0.1
MCP_PROXY_PORT=80

# 证书目录（create 时生成/复用，只读挂载）——命名隧道（anthropic）模式下
# 用于本地 TLS 终结（证书由隧道属主掌控，对齐上游 security model）。
MCP_PROXY_TLS_CERT=/certs/server.crt
MCP_PROXY_TLS_KEY=/certs/server.key

LOG_LEVEL=info